{
  "name": "emberhacks-compat-layer",
  "private": true,
  "version": "0.1.0",
  "description": "Agent conversation-history compatibility layer: detects and normalizes past sessions from 36 AI coding assistants.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "scan": "node cli.js",
    "scan:fixtures": "node cli.js --fixtures",
    "scan:json": "node cli.js --json",
    "test": "node test/redact.test.js && node test/digest.test.js && node test/topics.test.js && node test/mock-vscode-chat.js",
    "digest": "node cli.js --digest",
    "topics": "node cli.js --topics",
    "docs:example": "node docs/generate-example.js"
  },
  "engines": {
    "node": ">=22.13"
  },
  "dependencies": {},
  "//": "No runtime dependencies on purpose: the whole layer is Node built-ins. The nested package.json exists so ESM is used for this folder without touching the Electron app's own package.json."
}
